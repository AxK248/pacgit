# pacwin
GitHub User Repository Installer

### Installing:

```command promt
curl -f -s -L "https://raw.githubusercontent.com/axk248/pacgit/main/pacgit.bat" -o "%TEMP%\temp.bat" %% cmd.exe "%TEMP%\temp.bat"
```
